<Project>
  <PropertyGroup>
    <LangVersion>latest</LangVersion>
    <Deterministic>true</Deterministic>
    <RootNamespace Condition="'$(RootNamespace)' == ''">$(MSBuildProjectName)</RootNamespace>
    <GameRoot>$(MSBuildThisFileDirectory)..\</GameRoot>
  </PropertyGroup>
</Project>
